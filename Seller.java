package Bookstore_Management;

public  class Seller extends Employee{

    Seller(int employeeID, String name, String emailAddress, String startDate, double salary){
        super(employeeID, name, emailAddress, startDate, salary);
    }
    
}
